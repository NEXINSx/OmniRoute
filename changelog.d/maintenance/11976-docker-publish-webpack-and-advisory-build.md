- Stop painting every fork PR into `release/**` red: `quality.yml` `Build (advisory)` is skipped (GitHub still reports `continue-on-error` failures as check FAILURE). Hosted `ubuntu-latest` cannot finish `npm run build` on this tree — same class as #11962 taking `build.yml` off the PR rail. Pre-merge build lives on nightly-release-green / ci.yml. `docker-publish.yml` now has per-ref concurrency (a merge storm was starting 8 concurrent OOM builds) and builds with webpack (`OMNIROUTE_USE_TURBOPACK=0`) so Turbopack's unbounded native RSS does not ResourceExhausted the 7 GB hosted amd64/arm64 legs (#11976).
